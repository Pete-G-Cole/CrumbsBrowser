<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CrumbsBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CrumbsBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CrumbsBrowser">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="titleBarGrid"
              Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="6">
                <Image x:Name="titleBarIcon" Width="16" Height="16" Visibility="Collapsed"/>
                <TextBlock x:Name="titleBarText"
                           VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}"/>
            </StackPanel>
        </Grid>

        <Border x:Name="addressBarBorder"
                Grid.Column="0" Grid.Row="1"
                Height="32" Margin="8,8,8,8"
                CornerRadius="16"
                BorderThickness="1.5"
                BorderBrush="#999999"
                VerticalAlignment="Center">
            <TextBox x:Name="addressBar"
                     Background="Transparent"
                     BorderThickness="0"
                     Padding="14,6,14,0"
                     VerticalAlignment="Center"
                     KeyDown="AddressBar_KeyDown"
                     GotFocus="AddressBar_GotFocus"
                     LostFocus="AddressBar_LostFocus"/>
        </Border>

        <WebView2 x:Name="WebBrowser" Grid.Row="2" Grid.ColumnSpan="2"
                  Source="about:blank" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" NavigationStarting="EnsureHttps"/>
    </Grid>
</Window>

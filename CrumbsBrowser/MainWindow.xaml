<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CrumbsBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CrumbsBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CrumbsBrowser">

    <Grid>
        <Grid.Resources>
            <SolidColorBrush x:Key="ButtonBackgroundPointerOver" Color="#22000000"/>
            <SolidColorBrush x:Key="ButtonBackgroundPressed"     Color="#38000000"/>
            
            <Style x:Key="NavButtonStyle" TargetType="Button">
                <Setter Property="Width"           Value="32"/>
                <Setter Property="Height"          Value="32"/>
                <Setter Property="Padding"         Value="0"/>
                <Setter Property="CornerRadius"    Value="4"/>
                <Setter Property="Background"      Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="titleBarGrid"
              Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="6">
                <Image x:Name="titleBarIcon" Width="16" Height="16" Visibility="Collapsed"/>
                <TextBlock x:Name="titleBarText"
                           VerticalAlignment="Center"
                           Style="{StaticResource CaptionTextBlockStyle}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Grid.ColumnSpan="2" Margin="4,4,8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal"
                        VerticalAlignment="Center" Spacing="2">
                <Button x:Name="backButton"
                        Style="{StaticResource NavButtonStyle}"
                        IsEnabled="False"
                        Click="BackButton_Click"
                        ToolTipService.ToolTip="Back">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE76B;" FontSize="14"/>
                </Button>
                <Button x:Name="forwardButton"
                        Style="{StaticResource NavButtonStyle}"
                        IsEnabled="False"
                        Click="ForwardButton_Click"
                        ToolTipService.ToolTip="Forward">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE76C;" FontSize="14"/>
                </Button>
                <Button x:Name="refreshButton"
                        Style="{StaticResource NavButtonStyle}"
                        IsEnabled="False"
                        Click="RefreshButton_Click"
                        ToolTipService.ToolTip="Refresh">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" FontSize="14"/>
                </Button>
            </StackPanel>

            <Border x:Name="addressBarBorder"
                    Grid.Column="1"
                    Height="28" Margin="4,4,4,4"
                    CornerRadius="14"
                    BorderThickness="1.5"
                    BorderBrush="#C8C8C8"
                    VerticalAlignment="Center">
                <TextBox x:Name="addressBar"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="12,6,12,0"
                         VerticalAlignment="Center"
                         KeyDown="AddressBar_KeyDown"
                         GotFocus="AddressBar_GotFocus"
                         LostFocus="AddressBar_LostFocus">
                    <TextBox.Resources>
                        <SolidColorBrush x:Key="TextControlBackgroundPointerOver" Color="#0F000000"/>
                        <SolidColorBrush x:Key="TextControlBackgroundFocused"     Color="Transparent"/>
                    </TextBox.Resources>
                </TextBox>
            </Border>
        </Grid>

        <WebView2 x:Name="WebBrowser" Grid.Row="2" Grid.ColumnSpan="2"
                  Source="about:blank" HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" NavigationStarting="EnsureHttps"/>
    </Grid>
</Window>

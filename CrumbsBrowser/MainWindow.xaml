<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="CrumbsBrowser.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CrumbsBrowser"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="CrumbsBrowser">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="addressBar" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center" KeyDown="AddressBar_KeyDown"/>
        <Button x:Name="goButton" Grid.Column="1" Grid.Row="0" Content="Go" Click="GoButton_Click"/>
        <WebView2 x:Name="WebBrowser"  Grid.Row="1" Grid.ColumnSpan="2"
        Source="https://www.meadroid.com" HorizontalAlignment="Stretch" 
        VerticalAlignment="Stretch" NavigationStarting="EnsureHttps"/>
    </Grid>
</Window>
